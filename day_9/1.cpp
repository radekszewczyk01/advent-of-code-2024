#include<bits/stdc++.h>

using namespace std;

int main(){
    ifstream input("input.txt");
    string linia;
    string l;
    int iterator=-1;
    int col_it=0;
    while(getline(input,linia)){
        l=linia;
    }
    for(int i=l.size()-1; i>1; ){
        int rozmiar=1;
        char obecny = l[i];
        while()
        for(int j=1; )
    }
    return 0;
}